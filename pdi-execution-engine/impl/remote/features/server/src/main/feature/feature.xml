<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<features xmlns="http://karaf.apache.org/xmlns/features/v1.4.0" name="${project.artifactId}">
  <!--<repository>mvn:org.apache.aries.rsa:repository:1.9.0</repository>-->

  <feature name="${project.artifactId}" description="${project.artifactId}" version="${project.version}">
    <feature dependency="true" version="4.0.7">scr</feature>
    <feature dependency="true" version="${aries.rsa.version}">aries-rsa-provider-fastbin</feature>
    <feature dependency="true" version="${aries.rsa.version}">aries-rsa-discovery-zookeeper</feature>
    <feature dependency="true" version="${aries.rsa.version}">aries-rsa-discovery-zookeeper-server</feature>
  </feature>
</features>
